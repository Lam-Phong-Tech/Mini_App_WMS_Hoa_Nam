<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Quét mã bằng camera (Prompt 3 §A).
      KHÔNG xin RECORD_AUDIO: nghiệp vụ kho chỉ đọc mã, không quay video có tiếng.
      `uses-feature camera required="false"` do chính manifest của
      react-native-vision-camera khai và được merge vào đây — giữ nguyên để máy
      PDA không có camera vẫn cài được app.
    -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Không xin runtime permission: NFC Reader Mode chỉ cần khai khả năng.
         `required=false` để PDA không có NFC vẫn cài và dùng các luồng kho khác. -->
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <!--
      Gỡ hai quyền do manifest của react-native-camera-kit tự khai.

      Chúng phục vụ tính năng CHỤP ẢNH của thư viện — thứ ứng dụng này KHÔNG
      dùng: `capture()` không được gọi ở bất kỳ đâu (xem CameraScanScreen.tsx).
      Xin quyền ghi bộ nhớ ngoài mà không bao giờ dùng là điểm trừ khi rà soát
      bảo mật nội bộ, và trái tinh thần Prompt 3 §A "Không lưu ảnh camera nếu
      nghiệp vụ không yêu cầu".

      ⚠️ Nếu sau này nghiệp vụ cần chụp ảnh hiện trạng hàng hoá thì phải bỏ hai
      dòng `tools:node="remove"` này, KHÔNG lách bằng cách khác.

      ✅ CẬP NHẬT 2026-09-06 — nghiệp vụ ĐÃ cần (ảnh/video hồ sơ bảo hành), và
      hoá ra **vẫn không phải bỏ hai dòng này**. Kiểm chứng:

      - `react-native-image-picker@8.2.1` khai manifest **không có quyền nào** —
        chỉ một FileProvider (`node_modules/.../android/src/main/AndroidManifest.xml`).
      - Nó chọn ảnh bằng **photo picker của Android** (`PickVisualMedia`,
        `ImagePickerModuleImpl.java:27,139-152`), cơ chế từ API 33 **không đòi
        quyền nào cả**. Không có lời gọi `checkSelfPermission` hay tham chiếu
        `READ_MEDIA_*` / `READ_EXTERNAL_*` nào trong module.
      - Chụp ảnh mới dùng `CAMERA` — quyền đã khai sẵn ở trên.

      ⇒ Hai quyền storage cũ vẫn **không dùng tới**, và với `targetSdk 36` thì
      Android bỏ qua chúng cho media. Khôi phục chúng chỉ để "cho chắc" sẽ là
      thêm quyền thừa vào bản rà soát bảo mật — đúng thứ ghi chú trên muốn tránh.
    -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:node="remove" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
