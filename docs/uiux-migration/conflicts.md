# UIUX-G1 conflicts requiring an explicit decision

Decision update recorded at `2026-09-08T18:23:39.7269178+07:00`: D05 is approved. D01–D04 and D06–D08 have an approved direction, but the authoritative schema, operating values, retention details, compatibility proof, numeric virtualisation threshold or device evidence named in the table are still missing. They continue to block only the work that depends on those missing inputs.

| ID | Evidence | Decision needed | Unsafe assumptions rejected |
| --- | --- | --- | --- |
| D01 | Designer request uses name/phone/`productIds[]`/note and an accepted receipt; target public contract uses one `product_id`, optional variant, consent/privacy, and `{request_id,status:RECEIVED}`. | Identify the authoritative quote endpoint/schema/version. If multi-product is required, provide the approved API and backend-change authority. | Fan-out to N requests, silently use the first product, or change body/response locally. |
| D02 | Designer and target differ on note limit, phone format, timeout/retry, consent and privacy handling. Target currently has 500 characters, a strict Vietnamese-mobile pattern, 8-second timeout and one retry with the same idempotency key. | Approve exact client/server validation, timeout, retry/idempotency and consent/privacy-version behavior. | Alter validation/retry only to imitate a design form. |
| D03 | Designer source has design asset/local data and documents a hotline/OA; target requires public config and public DTO media. | Supply approved per-environment public contact/config values, product/media source or a separately approved non-production QA fixture, and asset-use rights. | Hard-code a source hotline/OA, copy a design image into runtime, or reclassify demo data as operating data. |
| D04 | PV-13–PV-18 add saved/recent/compare/multi-selection/receipt/FAQ; target has no routes for them and scope forbids order history/offline catalogue. | Approve persistence/storage/retention limits and clarify that same-session receipt is not order tracking. Approve or reject multi-product scope after D01. | Store a whole catalogue offline, retain PII, or present request history as server truth. |
| D05 | Older Designer handoff content conflicts with newer source/header/hero/token files; live HTTP is reachable but not visually captured or tied to the locked commit. | **Resolved:** locked source snapshot `86079f965f2fcb43a7e3efbbc9467d119b47921f` is the implementation/acceptance baseline; live is reference-only. | Mix generations or treat HTTP 200 as visual verification. |
| D06 | Designer is React 19/Vite 8/window-scroll and names Motion, GSAP, Locomotive and virtualizer; target is React 18/Vite 5/ZaUI Page with a legacy browserslist. | Approve scroll ownership only after an actual target measurement. Approve each compatible dependency/version or a native-CSS fallback. | Upgrade framework majors, intercept all scrolling, or install packages merely because Designer uses them. |
| D07 | “100%” needs a locked route/data/viewport/DPR/browser/OS/font/asset/scroll/animation protocol. No target-device or screenshot evidence exists. | Approve the device/browser matrix, source of comparison data/assets, route states and exact pixel/exception rule. | Claim pixel match, choose tolerance, or infer Zalo safe-area/IME behavior from desktop. |
| D08 | Designer virtualizer source is fixed two-column rows while its document calls for four desktop columns; target supports older browsers. | Resolve intended desktop layout and supported-device impact after a >=80-item test case is supplied. | Quietly change grid semantics or drop browser support. |
