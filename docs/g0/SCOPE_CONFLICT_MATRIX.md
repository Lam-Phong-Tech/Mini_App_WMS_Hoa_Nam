# SCOPE CONFLICT MATRIX — G0

| ID | Evidence | Conflict / risk | G0 decision |
| --- | --- | --- | --- |
| C-01 | SRS executive summary, Figure 1, ADR-04, ADR-05, FR-013, UAT-13/15 | Lead API writes to CRM/ERP through outbox, queue, retry, dead-letter, alert, and notification. | **Deprecated for current MVP.** Quote ends after minimal durable save and `RECEIVED`. No CRM/ERP, outbox, queue, retry, DLQ, notification, dashboard, export, assignment, or lifecycle sync enters code, schema, config, or API. |
| C-02 | SRS quote sample includes `source_context`, Lead status, trace information; prompt gives an exact quote schema | Extra request/persistence fields would become CRM/lifecycle surface or exceed the permitted schema. | Persist only the 13 allowed columns. `consent` is an input used to derive `consent_at`; `source` is server-derived as `ZALO_MINI_APP`; `RECEIVED` is response-only. No `source_context`, status, trace, CRM, sync, retry, or outbox columns. |
| C-03 | SRS table 12 exposes internal `publish_status`, `source_reference`; SRS table 13 includes barcode; scope lock allows public-only data | Returning internal data or unknown codes leaks information and invites fabricated data. | Public DTOs omit `publish_status`, source references, supplier/internal notes, stock quantity, barcode, and unapproved codes. Model/approved primary item code are searchable; barcode can only be indexed server-side if a real source supplies it. |
| C-04 | SRS says only PUBLISHED/IN_STOCK; its DTO table also lists OUT_OF_STOCK; prototype shows “Còn hàng” | A client-visible status catalog can disclose unavailable/internal records. | All list/home/category/related results are eligible public records only. The public DTO may say `availability: IN_STOCK`, never a quantity or `OUT_OF_STOCK`. Detail lookup for an ineligible record is an indistinguishable 404; the Mini App maps it to Product Unavailable. Quote may return `PRODUCT_NOT_AVAILABLE` if availability changes after the detail was opened. |
| C-05 | SRS source readiness table; prompt forbids invented SKU/barcode/spec/media/UAT data | Prototype embeds 12 product samples, base64 media, source strings, and illustrative SKU-like terms. | Prototype is UI/UX-only. No sample record, base64 asset, internal source string, phone, media, SKU, barcode, or spec is copied into DEV fixture, UAT, or Production data. Empty states are contract-valid. |
| C-06 | SRS taxonomy labels; prototype uses display IDs `Power Tools`, `Hand Tools`, `Accessories`; prompt requires uppercase domain codes | Display strings are not stable machine taxonomy. | API uses `POWER_TOOLS`, `HAND_TOOLS`, `ACCESSORIES`. Human labels come from public taxonomy DTO. Proposed stable category codes are explicitly pending BA/Data Entry approval. |
| C-07 | SRS FR-005 permits model/SKU/barcode search; prompt mandates exact model/item-code before fuzzy name | SKU is missing for most cited data and must not be inferred. | Normalize and search real indexed model/item-code (and optional sourced barcode only on server) before diacritic-insensitive fuzzy name. UI/API never require or synthesize SKU/barcode. |
| C-08 | SRS references entry via QR; prompt excludes QR/NFC | Platform access mechanics could be mistaken for a feature. | Do not build QR generation/scanning/NFC routes or APIs. Share/deep link is in scope. An externally provided Zalo link/QR may open the app, but it has no dedicated product feature. |
| C-09 | SRS requires event invalidation/cache/SLA; prompt excludes queues only for CRM/ERP/outbox/retry/notification | Committing an unapproved backend architecture in G0 would expand scope. | Contract exposes only public version/data metadata and refetch behavior. Catalogue publication implementation/SLA is an integration question, not a G0 backend build; it must not introduce CRM/Lead queue infrastructure. |
| C-10 | SRS FR-016 lists analytics and `POST /events`; requested OpenAPI endpoint list omits it | Adding an endpoint exceeds the requested public contract and risks PII. | No `/events` endpoint in OpenAPI v1. G4 may assess PII-safe client/platform analytics separately; quote PII is never analytics/log payload. |
| C-11 | Prototype omits a full quote screen; SRS/prompt require minimal quote request | UI baseline is incomplete for a required MVP conversion flow. | Keep prototype styling/layout as a visual baseline; define quote contract in G0 and implement only in G3 after G1/G2 gates. |
| C-12 | Current source has a single Hello World route and hard-coded component-library `appId`; SRS expects dynamic config and many screens | Repository state is not the product baseline and hard-coded operations conflict with dynamic config. | Record as G1 work only. No current source change in G0. Hotline/OA/API values must come from public config/build keys with no secret values. |
| C-13 | SRS gate table includes G5 release; user requests requirement mapping only G0–G4 and says do not run Prompt 02 | Release work could be started prematurely. | This artifact maps G0–G4 only. No G1–G4 implementation, no G5 planning action, and no deployment/publish occurs in this run. |
